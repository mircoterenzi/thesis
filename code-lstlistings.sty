\usepackage{listings}
\usepackage{xcolor}

%-----------------------------------------------------
% LISTING CONFIGURATIONS
%-----------------------------------------------------

\crefname{lstlisting}{listato}{listato}	
\Crefname{lstlisting}{Listato}{Listato}
\renewcommand\lstlistlistingname{Elenco dei listati}
\renewcommand{\lstlistingname}{Listato}

\lstset{
	showstringspaces=false,
	basicstyle=\scriptsize\ttfamily,
	commentstyle={\color{myGreen}},
	identifierstyle=\color{black},
	ndkeywordstyle={\color{blue}},
	stringstyle={\color{myGreen}},
	emphstyle={},
	keywordstyle={\color{blue}},
	breaklines=true,
	numbers=none,
	numberstyle=\tiny\color{gray},
	numbersep=10pt,
	tabsize=2,
	extendedchars=true,
	frame=none,
	captionpos=b
}

\lstdefinelanguage{Kotlin}{
	comment=[l]{//},
	emph={delegate, filter, first, firstOrNull, forEach, lazy, map, mapNotNull, println, return@},
	keywords={abstract, actual, as, as?, break, by, class, companion, continue, data, do, dynamic, else, enum, expect, false, final, for, fun, get, if, import, in, interface, internal, is, null, object, override, package, private, public, return, set, super, suspend, this, throw, true, try, typealias, val, var, vararg, when, where, while},
	morecomment=[s]{/*}{*/},
	morestring=[b]",
	morestring=[s]{"""*}{*"""},
	ndkeywords={@Deprecated, @JvmField, @JvmName, @JvmOverloads, @JvmStatic, @JvmSynthetic, Array, Byte, Double, Float, Int, Integer, Iterable, Long, Runnable, Short, String, Unit, it},
	sensitive=true
}

\definecolor{myPink}{HTML}{F04265}
\definecolor{myPurple}{HTML}{714FCF}
\definecolor{myGreen}{HTML}{5E9785}

\lstdefinelanguage{BNF}{
	morekeywords={::=,|},
	sensitive=false,
	morestring=[b]",
	literate={->}{$\rightarrow$}{2},
	stringstyle=\color{myPink}
}

\lstdefinelanguage{SQL}{
	keywords={SELECT, FROM, WHERE, INSERT, INTO, VALUES, UPDATE, SET, DELETE, CREATE, TABLE, ALTER, 
			ADD, DROP, JOIN, INNER, LEFT, RIGHT, FULL, ON, GROUP, BY, ORDER, ASC, DESC, COUNT, SUM, AVG, MIN, 
			MAX, AS, DISTINCT, IN, AND, OR, NOT, NULL, LIKE, IS, BETWEEN, UNION, ALL, EXISTS, ANY, HAVING, LIMIT,
			PRIMARY, KEY},
	keywordstyle=\color{myPurple}\bfseries,
	ndkeywords={int, varchar, char, date, float, boolean},
	ndkeywordstyle=\color{myPink}\bfseries,
	identifierstyle=\color{black},
	sensitive=false,
	comment=[l]{--},
	morecomment=[s]{/*}{*/},
	morestring=[b]',
	morestring=[b]",
	stringstyle=\color{myGreen}
}

\lstdefinelanguage{Java}{
	keywords={abstract, assert, boolean, break, byte, case, catch, char, class, const, continue, default, do, 
			double, else, enum, extends, final, finally, float, for, goto, if, implements, import, instanceof, 
			int, interface, long, native, new, null, package, private, protected, public, return, short, static, 
			strictfp, super, switch, synchronized, this, throw, throws, transient, try, void, volatile, while, 
			true, false},
	keywordstyle=\color{myPurple}\bfseries,
	ndkeywords={String, Integer, Double, Boolean, Character, Long, Short, Byte, Exception, System, println,
			ASTNode, File, FileUtils, DataTests, Exception, Assertions, URISyntaxException, URL, Thread, Objects,
			PrintStream, ByteArrayOutputStream, StandardCharsets},
	ndkeywordstyle=\color{myPink}\bfseries,
	comment=[l]{//},
	morecomment=[s]{/*}{*/},
	commentstyle=\color{myGreen}\ttfamily,
	stringstyle=\color{myGreen}\ttfamily,
	sensitive=true,
	morestring=[b]",
}

\lstdefinelanguage{Neverlang}{
	keywords={module, slice, with, role},
	keywordstyle=\color{myPurple}\bfseries,
	ndkeywords={\$1, \$5, \$2, \$3, \$6, \$4},
	ndkeywordstyle=\color{myPink}\bfseries,
	stringstyle=\color{myPink}\ttfamily,
	sensitive=true,
	morestring=[b]',
	literate={<--}{{$\leftarrow$}}1
}